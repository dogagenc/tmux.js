# Intentionally empty: integration tests load this via `tmux -f` so the server
# starts from tmux's built-in defaults, isolated from the developer's
# ~/.tmux.conf (base-index and other options must not leak into tests).
